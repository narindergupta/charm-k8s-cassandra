# Copyright 2020 Canonical Ltd.
# See LICENSE file for licensing details.
#
# This action expects the python3 package in the image you are deploying.
status:
  description: Returns a cluster status.
  params:
    command:
      description: |
          "Return the result of nodetool command. This action takes
           the argument for nodetool command."
      type: string
      default: "status"
